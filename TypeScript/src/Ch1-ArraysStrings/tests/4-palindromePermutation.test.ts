import { palindromePermutation } from '../4-palindromePermutation';

describe('palindrome permutation', () => {
  test('using a hashmap', () => {
    expect(palindromePermutation('Tact Coa')).toBe(true);
  });
});
